@model IEnumerable<FreteFree.Models.Motorista.OrdemCarregamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MotoristaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataOrdemCarregamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ativo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa.CidadeEmpresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MotoristaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Medida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataOrdemCarregamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ativo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OrdemCarregamentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OrdemCarregamentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OrdemCarregamentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
